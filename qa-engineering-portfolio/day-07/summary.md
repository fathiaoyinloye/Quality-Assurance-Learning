Today I learned that APIs act as the communication layer between systems by accepting requests and returning responses. I learned the difference between GET, POST, PUT, and DELETE requests and how status codes indicate whether a request succeeded or failed. Through the SwiftBank examples, I realized that QA engineers should not only test the user interface but also verify that backend APIs enforce business rules, validation, and security requirements.